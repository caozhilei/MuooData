# 流程实例页面调试指南

## 问题现象
流程实例页面没有内容显示

## 可能的原因

### 1. 没有流程实例数据（最常见）
- **现象**: 页面正常加载，但列表为空
- **原因**: 系统中没有运行中的流程实例
- **解决**: 这是正常的，需要先创建流程定义并启动流程实例

### 2. API请求失败
- **检查方法**: 打开浏览器控制台（F12）→ Network 标签
- **查看**: 查找 `/workflow/instances/pageRunning` 请求
- **检查**: 
  - 状态码是否为 200
  - 响应数据格式是否正确
  - 是否有错误信息

### 3. 前端代码问题
- **检查方法**: 打开浏览器控制台（F12）→ Console 标签
- **查看**: 是否有 JavaScript 错误
- **已添加**: 调试日志会输出到控制台

## 调试步骤

### 步骤1: 检查浏览器控制台
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 刷新页面
4. 查看是否有以下日志：
   - `流程实例列表响应:` - 显示API响应数据
   - `流程实例数据:` - 显示解析后的数据
   - `设置后的数据:` - 显示最终设置到页面的数据

### 步骤2: 检查网络请求
1. 切换到 Network 标签
2. 刷新页面
3. 查找 `/workflow/instances/pageRunning` 请求
4. 点击查看：
   - **Request Headers**: 确认有 `Authorization` token
   - **Response**: 查看返回的数据格式
   - **Status**: 确认状态码为 200

### 步骤3: 验证后端接口
```bash
# 测试接口是否正常
curl "http://localhost:9538/workflow/instances/pageRunning?pageNum=1&pageSize=20"
```

预期响应：
```json
{
  "success": true,
  "code": 200,
  "msg": "操作成功",
  "data": {
    "total": "0",
    "data": [],
    "pageSize": 20,
    "pageNum": 1
  }
}
```

### 步骤4: 检查是否有数据
如果 `total` 为 `"0"` 或 `0`，说明没有流程实例数据。

**创建测试数据的步骤**:
1. 访问"流程定义"页面
2. 创建或导入一个流程定义
3. 启动流程实例
4. 返回"流程实例"页面查看

## 常见问题

### Q1: 页面一直显示加载中
- **原因**: API请求失败或超时
- **解决**: 检查网络请求，查看错误信息

### Q2: 页面显示但列表为空
- **原因**: 没有流程实例数据（正常情况）
- **解决**: 创建流程定义并启动流程实例

### Q3: 控制台有错误信息
- **原因**: 前端代码错误或API返回格式异常
- **解决**: 根据错误信息定位问题

## 调试日志说明

已在前端代码中添加了调试日志，会在浏览器控制台输出：
- `流程实例列表响应:` - API的完整响应
- `流程实例数据:` - 解析后的数据对象
- `设置后的数据:` - 最终设置到页面的数据

这些日志可以帮助快速定位问题。

## 测试数据创建

如果需要测试，可以：
1. 访问"流程定义"页面
2. 创建一个简单的流程定义
3. 启动流程实例
4. 返回"流程实例"页面查看数据

## 相关接口

- **运行中的流程实例**: `/workflow/instances/pageRunning`
- **我发起的流程实例**: `/workflow/instances/pageMyStarted`
- **我参与的流程实例**: `/workflow/instances/pageMyInvolved`
- **待办任务**: `/workflow/tasks/pageTodo`
- **已办任务**: `/workflow/tasks/pageDone`

