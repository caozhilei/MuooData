# 推送到 GitHub 指南

## 📋 当前状态

✅ 已完成：
- 所有代码已提交到本地仓库
- 远程仓库已配置为：`git@github.com:andyapple/MuooData.git`
- SSH 密钥已生成

## 🚀 推送步骤（选择一种方式）

### 方式 1：使用 SSH（推荐，一次配置永久使用）

#### 步骤 1：添加 SSH 公钥到 GitHub

**你的 SSH 公钥：**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIL8XJGnaByObOu9EXxboZ+o0l57y/on1KSJurhII9Nuc andyapple@github
```

1. 复制上面的公钥内容
2. 访问：https://github.com/settings/keys
3. 点击 "New SSH key"
4. Title 填写：`MuooData MacBook`（或任意名称）
5. Key 粘贴上面的公钥
6. 点击 "Add SSH key"

#### 步骤 2：推送代码

```bash
cd /Users/andyapple/Documents/Coding/alldata
git push -u origin master
```

---

### 方式 2：使用 Personal Access Token（简单快速）

#### 步骤 1：创建 Token

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. Note 填写：`MuooData Push`
4. 过期时间：选择你需要的期限（建议 90 天或 No expiration）
5. 权限：勾选 `repo`（完整仓库权限）
6. 点击 "Generate token"
7. **重要**：复制生成的 token（只显示一次，请保存好）

#### 步骤 2：推送代码

```bash
cd /Users/andyapple/Documents/Coding/alldata
git remote set-url origin https://github.com/andyapple/MuooData.git
git push -u origin master
```

推送时会提示：
- Username: `andyapple`
- Password: 粘贴你的 Personal Access Token（不是 GitHub 密码）

---

## ✅ 验证

推送成功后，访问以下地址查看你的代码：
https://github.com/andyapple/MuooData

## 📝 注意事项

1. **确保 GitHub 上已创建仓库**：
   - 访问：https://github.com/new
   - 仓库名：`MuooData`
   - 不要初始化 README、.gitignore 或 license

2. **如果推送失败**：
   - 检查网络连接
   - 确认仓库已创建
   - 确认身份验证配置正确

3. **后续推送**：
   - 配置完成后，后续只需执行 `git push` 即可

