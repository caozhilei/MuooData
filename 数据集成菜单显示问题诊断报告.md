# 数据集成菜单显示问题诊断报告

## 问题描述

主页上看不到数据集成主菜单，虽然SQL权限已经正确分配。

## 问题分析

### 1. 前端菜单显示逻辑

**文件**: `moat_ui/src/layout/components/Sidebar/SidebarItem.vue`

```vue
<div v-if="!item.hidden" class="menu-wrapper">
```

- 如果菜单项的 `hidden` 属性为 `true`，则不会显示
- 菜单数据来自 `sidebarRouters` (Vuex Store)

### 2. 菜单数据流程

```
用户登录 
  → 调用 /system/api/menus/build
  → MenuController.buildMenus()
  → MenuServiceImpl.findByUser(userId) [带缓存]
  → MenuRepository.findByRoleIdsAndTypeNot(roleIds, 2)
  → 返回菜单列表
  → MenuServiceImpl.buildTree() 构建菜单树
  → MenuServiceImpl.buildMenus() 转换为前端格式
  → 前端 filterAsyncRouter() 处理
  → 存储到 Vuex sidebarRouters
  → SidebarItem 渲染菜单
```

### 3. 后端菜单查询SQL

**文件**: `MenuRepository.java`

```java
@Query(value = "SELECT m.* FROM sys_menu m, sys_roles_menus r WHERE " +
        "m.menu_id = r.menu_id AND r.role_id IN ?1 AND type != ?2 order by m.menu_sort asc",
        nativeQuery = true)
LinkedHashSet<Menu> findByRoleIdsAndTypeNot(Set<Long> roleIds, int type);
```

- SQL查询逻辑正确
- 测试SQL能正确返回数据集成菜单及其子菜单

### 4. 缓存机制

**文件**: `MenuServiceImpl.java`

```java
@Override
@Cacheable(key = "'user:' + #p0")
public List<MenuDto> findByUser(Long currentUserId) {
    // ...
}
```

- **问题根源**: 后端使用了Redis缓存
- 缓存key格式: `menu:user:{userId}`
- 即使SQL更新了权限，缓存中仍是旧数据（不包含数据集成菜单）

## 验证结果

### 数据库检查

```sql
-- 权限分配检查
SELECT COUNT(*) FROM sys_roles_menus 
WHERE menu_id IN (159,160,161,162,163,164,165,166,167,168,169) AND role_id=1;
-- 结果: 11条记录 ✅

-- 菜单查询SQL测试
SELECT m.menu_id, m.title, m.path, m.hidden, m.type 
FROM sys_menu m, sys_roles_menus r 
WHERE m.menu_id = r.menu_id AND r.role_id = 1 AND m.type != 2 
AND (m.menu_id = 159 OR m.pid = 159) 
ORDER BY m.menu_sort ASC;
-- 结果: 11条记录（1个主菜单 + 10个子菜单）✅

-- hidden字段检查
SELECT menu_id, title, hidden FROM sys_menu WHERE menu_id=159;
-- 结果: hidden = false (0) ✅
```

### 前端代码检查

- `SidebarItem.vue`: 使用 `v-if="!item.hidden"` 控制显示 ✅
- `filterAsyncRouter`: 不会过滤掉 `hidden=false` 的菜单 ✅
- 菜单数据来源: `/system/api/menus/build` ✅

## 解决方案

### 方案1: 清除Redis缓存（已执行）

```bash
# 清除所有菜单相关缓存
docker exec alldata-redis redis-cli --scan --pattern "menu:*" | xargs docker exec alldata-redis redis-cli DEL

# 清除用户相关缓存
docker exec alldata-redis redis-cli --scan --pattern "*user*" | xargs docker exec alldata-redis redis-cli DEL
```

### 方案2: 重新登录系统

1. 退出当前登录
2. 重新登录前端系统
3. 后端会重新查询数据库并缓存新数据

### 方案3: 重启系统服务（如果方案1和2无效）

```bash
# 重启系统服务，清除所有缓存
# 需要找到系统服务的启动脚本并重启
```

## 代码位置

### 后端

- **菜单服务**: `moat/studio/system-service-parent/system-service/src/main/java/com/platform/modules/system/service/impl/MenuServiceImpl.java`
  - `findByUser()`: 第84-90行（带缓存）
  - `buildMenus()`: 第244-294行
- **菜单仓库**: `moat/studio/system-service-parent/system-service/src/main/java/com/platform/modules/system/repository/MenuRepository.java`
  - `findByRoleIdsAndTypeNot()`: 第52-54行
- **菜单控制器**: `moat/studio/system-service-parent/system-service/src/main/java/com/platform/modules/system/rest/MenuController.java`
  - `buildMenus()`: 第49-55行

### 前端

- **菜单API**: `moat_ui/src/api/system/menu.js`
  - `buildMenus()`: 第34-39行
- **路由处理**: `moat_ui/src/router/index.js`
  - `loadMenus()`: 第13-27行
- **权限管理**: `moat_ui/src/store/modules/permission.js`
  - `filterAsyncRouter()`: 第30-53行
- **侧边栏组件**: `moat_ui/src/layout/components/Sidebar/SidebarItem.vue`
  - 菜单显示逻辑: 第2行

## 总结

**问题根源**: Redis缓存导致菜单数据未更新

**解决状态**: 
- ✅ SQL权限已正确分配
- ✅ 数据库查询正常
- ✅ Redis缓存已清除
- ⏳ 需要重新登录前端系统以加载新数据

**预期结果**: 重新登录后，数据集成菜单将出现在左侧导航栏顶部。

