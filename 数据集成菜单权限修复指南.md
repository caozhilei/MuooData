# 数据集成菜单权限修复指南

## 问题描述

数据集成菜单（menu_id=159）及其10个子菜单在前端首页不显示。

## 问题原因

数据集成菜单在数据库 `sys_menu` 表中已存在，但在 `sys_roles_menus` 表中没有权限分配记录。系统通过 `sys_roles_menus` 表控制菜单的显示权限，只有分配给用户角色的菜单才会在前端显示。

## 解决方案

### 方法1: 使用简化SQL脚本（推荐）

```bash
mysql -uroot -p123456 -D alldata < fix-dts-menu-permission-simple.sql
```

### 方法2: 使用完整SQL脚本

```bash
mysql -uroot -p123456 -D alldata < fix-dts-menu-permission.sql
```

### 方法3: 手动执行SQL

连接到MySQL数据库后执行：

```sql
USE alldata;

-- 为管理员角色分配数据集成菜单权限
INSERT INTO sys_roles_menus (menu_id, role_id) VALUES
(159, 1),  -- 数据集成主菜单
(160, 1),  -- 数据源管理
(161, 1),  -- 项目管理
(162, 1),  -- 资源管理
(163, 1),  -- 任务详情
(164, 1),  -- 任务模板
(165, 1),  -- 运行日志
(166, 1),  -- 单任务
(167, 1),  -- 多任务
(168, 1),  -- 任务管理
(169, 1);  -- 注册中心
```

## 菜单ID对应关系

| 菜单ID | 菜单名称 | 路径 |
|--------|----------|------|
| 159 | 数据集成（主菜单） | /dts |
| 160 | 数据源管理 | /dts/jdbc-datasource |
| 161 | 项目管理 | /dts/jobProject |
| 162 | 资源管理 | /dts/resource |
| 163 | 任务详情 | /dts/jobInfo |
| 164 | 任务模板 | /dts/jobTemplate |
| 165 | 运行日志 | /dts/jobLog |
| 166 | 单任务 | /dts/json-build |
| 167 | 多任务 | /dts/json-build-batch |
| 168 | 任务管理 | /dts/executor |
| 169 | 注册中心 | /dts/registry |

## 执行后操作

1. **清除缓存**（如果系统使用Redis缓存菜单数据）
   ```bash
   # 清除用户菜单缓存
   redis-cli
   > KEYS menu:user:*
   > DEL menu:user:*
   ```

2. **重新登录前端系统**
   - 退出当前登录
   - 重新登录
   - 数据集成菜单将出现在左侧导航栏顶部

3. **验证菜单显示**
   - 登录后检查左侧导航栏
   - 应该能看到"数据集成"菜单（图标：database）
   - 点击后展开显示10个子菜单项

## 验证SQL

执行以下SQL验证权限分配是否成功：

```sql
SELECT rm.role_id, r.name as role_name, rm.menu_id, m.title as menu_title 
FROM sys_roles_menus rm 
JOIN sys_role r ON rm.role_id = r.role_id 
JOIN sys_menu m ON rm.menu_id = m.menu_id 
WHERE m.menu_id IN (159, 160, 161, 162, 163, 164, 165, 166, 167, 168, 169)
ORDER BY rm.role_id, rm.menu_id;
```

应该返回11条记录（1个主菜单 + 10个子菜单）。

## 技术说明

### 菜单加载机制

1. 前端调用 `/system/api/menus/build` 接口
2. 后端 `MenuController.buildMenus()` 方法处理请求
3. `MenuServiceImpl.findByUser()` 根据当前用户ID查询菜单
4. 通过 `MenuRepository.findByRoleIdsAndTypeNot()` 查询用户角色有权限的菜单
5. SQL查询：`SELECT m.* FROM sys_menu m, sys_roles_menus r WHERE m.menu_id = r.menu_id AND r.role_id IN ?1 AND type != ?2`
6. 只有 `sys_roles_menus` 表中有记录的菜单才会返回给前端

### 相关代码文件

- **后端菜单服务**: `moat/studio/system-service-parent/system-service/src/main/java/com/platform/modules/system/service/impl/MenuServiceImpl.java`
- **菜单控制器**: `moat/studio/system-service-parent/system-service/src/main/java/com/platform/modules/system/rest/MenuController.java`
- **菜单仓库**: `moat/studio/system-service-parent/system-service/src/main/java/com/platform/modules/system/repository/MenuRepository.java`

## 注意事项

1. 如果为普通用户角色（role_id=2）也需要访问数据集成菜单，需要额外执行：
   ```sql
   INSERT INTO sys_roles_menus (menu_id, role_id) VALUES
   (159, 2), (160, 2), (161, 2), (162, 2), (163, 2), (164, 2), 
   (165, 2), (166, 2), (167, 2), (168, 2), (169, 2);
   ```

2. 如果菜单仍然不显示，检查：
   - 用户是否具有正确的角色
   - Redis缓存是否已清除
   - 前端是否正确调用菜单API
   - 浏览器控制台是否有错误信息

3. 菜单排序：数据集成菜单的 `menu_sort=1`，会显示在导航栏最顶部位置。

