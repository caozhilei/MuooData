# 清除菜单缓存方案

## 问题

数据集成菜单不显示，原因是Redis缓存中存储的是旧数据（不包含数据集成菜单）。

## 解决方案

### 方案1: 重新登录（最简单，推荐）

**步骤**:
1. 退出当前登录
2. （可选）清除浏览器缓存或使用无痕模式
3. 重新登录前端系统: http://localhost:8013
4. 后端会重新查询数据库并缓存新数据
5. 数据集成菜单应该会显示

**原理**: 
- 重新登录会触发新的菜单查询
- 后端会重新执行 `findByUser()` 方法
- 由于缓存key包含userId，新登录会生成新的缓存key
- 或者缓存会自动失效并重新加载

### 方案2: 重启系统服务

**步骤**:
1. 找到系统服务的启动脚本或进程
2. 停止系统服务
3. 重新启动系统服务
4. 重新登录前端系统

**命令**:
```bash
# 查找系统服务进程
ps aux | grep SystemServiceApplication

# 停止服务（根据实际情况）
kill <PID>

# 重新启动（根据启动脚本）
./start-system-service.sh
```

### 方案3: 手动清除Redis缓存

**前提**: 需要Redis密码

**步骤**:
1. 获取Redis密码（从配置文件或环境变量）
2. 连接Redis并清除缓存

**命令**:
```bash
# 如果Redis没有密码
docker exec alldata-redis redis-cli FLUSHDB

# 如果有密码（假设密码为空或123456）
docker exec alldata-redis redis-cli -a "" FLUSHDB
# 或
docker exec alldata-redis redis-cli -a "123456" FLUSHDB

# 或者只清除菜单相关缓存
docker exec alldata-redis redis-cli --scan --pattern "menu:*" | xargs docker exec alldata-redis redis-cli DEL
```

### 方案4: 通过后端API清除缓存

如果后端提供了清除缓存的API，可以通过API调用清除。

## 验证

清除缓存后，重新登录系统，检查：
1. 左侧导航栏是否显示"数据集成"菜单
2. 菜单图标是否为database
3. 点击后是否能展开显示10个子菜单

## 技术说明

### 缓存机制

**后端代码**: `MenuServiceImpl.java`

```java
@Override
@Cacheable(key = "'user:' + #p0")
public List<MenuDto> findByUser(Long currentUserId) {
    // 查询用户菜单
}
```

- **缓存key格式**: `menu:user:{userId}`
- **缓存位置**: Redis
- **缓存时机**: 首次查询后缓存
- **缓存失效**: 用户角色变更或菜单变更时自动清除

### 菜单查询流程

```
用户登录
  → 调用 /system/api/menus/build
  → MenuServiceImpl.findByUser(userId)
  → 检查Redis缓存 (key: menu:user:{userId})
  → 如果缓存存在，直接返回缓存数据 ❌ (旧数据)
  → 如果缓存不存在，查询数据库 ✅ (新数据)
  → 缓存查询结果
  → 返回给前端
```

## 预防措施

为了避免类似问题，建议：
1. 在分配菜单权限后，自动清除相关用户的菜单缓存
2. 或者在菜单权限变更时，清除所有用户的菜单缓存
3. 或者在菜单查询时，检查权限变更时间，如果权限已更新则清除缓存

